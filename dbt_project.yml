name: 'codegen'
version: '0.4.0'

require-dbt-version: [">=0.18.0", "<1.1.0"]
config-version: 2

target-path: "target"
clean-targets: ["target", "dbt_modules"]
macro-paths: ["macros"]
log-path: "logs"
