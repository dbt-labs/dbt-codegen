name: 'codegen_integration_tests'
version: '1.0'
config-version: 2

profile: 'integration_tests'

models-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_packages"

seeds:
  +schema: raw_data
  +quote_columns: false
